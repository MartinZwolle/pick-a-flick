Pick a Flick M3 update

Changed:
- richer 'Mijn filmsmaak' profile page
- favorites and rewatchable poster sections
- all ratings remain editable
- per-profile veto overview
- deterministic taste summary for genres, directors and actors
- ability to remove a movie/rating from a profile
- no database migration required

Install:
Extract this zip over the root of your local pick-a-flick repository.
Then:
  git status
  git add .
  git commit -m "Add My Taste profile M3"
  git push origin main

Komodo:
Deploy as usual. With Pre Build Images enabled, it rebuilds the app automatically.
